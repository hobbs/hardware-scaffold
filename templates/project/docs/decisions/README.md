# Decision records

Create a record for choices that are costly to reverse, cross domain boundaries,
or would otherwise invite the same debate later. Copy `0000-template.md` to the
next number, for example `0001-power-architecture.md`, and reference it as
`DEC-001`.
